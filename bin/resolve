#!/bin/sh
#
# .dots/bin/resolve

usage() {
    base=$(basename "$0")

    cat >&2 << EOF
Usage:
    $base
EOF

    [ $# -eq 0 ] || exit "$1"
}

main() {
    HOME=/home/fyr/.resolve || exit 1

    QT_QPA_PLATFORM=xcb /opt/resolve/bin/resolve
}

main "$@"
