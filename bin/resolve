#!/bin/sh
#
# .dots/bin/resolve

usage() {
    base=$(basename "$0")

    cat >&2 << EOF
Usage:
    $base
EOF

    [ $# -eq 0 ] || exit "$1"
}

main() {
    HOME=/home/fyr/.resolve || exit 1

    /opt/resolve/bin/resolve
}

main "$@"
